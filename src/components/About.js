import React from "react"
// import noteContext from "../context/notes/noteContext"

const About = () => {
  // const a= useContext(noteContext);
  // useEffect(()=>{
  //   a.update();  
  //   // if we export function, we can use that function to update our state
  // })



  return (
    // <div>
    //   Hi, this is {a.state.name} and he is in {a.state.class}
    // </div>
    <div>
      this is About page
    </div>
  )
}

export default About
